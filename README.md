# FoundryDeck Module

The Foundry VTT component of **FoundryDeck**. This module allows you to trigger automated actions, run macros, toggle playlists, play sounds, and execute generic scripts remotely. 

It connects to the `foundrydeck-relay` server using WebSockets, listening for incoming commands issued by a remote source such as a Stream Deck through the companion app.
